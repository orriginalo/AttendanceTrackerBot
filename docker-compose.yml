services:
  bot:
    container_name: attendancebot_bot
    build: 
      context: .
      dockerfile: Dockerfile
    volumes:
      - ./data/database.db:/app/data
    env_file:
      - .env
    environment:
      - TZ=Europe/Samara
    restart: unless-stopped
    